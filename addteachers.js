// const addTeachers = () => {
//   fetch ("geojson.json").then((response) => response.json()).then((data) => {
//     for (const feature of data.feature) {
//       const teacher = feature.properties.title;
//       const teacherList = document.querySelector(".dropdown-select .dropdown-menu");
//       const teacherItem = `<li><a class="dropdown-item" id="selected">${teacher}</a></li>`;
//       teacherList.insertAdjacentHTML("beforeend", teacherItem);
//     }
//   });
// }

// addTeachers();

// export { addTeachers };
